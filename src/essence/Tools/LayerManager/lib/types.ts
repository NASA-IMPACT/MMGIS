// The legend model lives in `_shared/legend/`, shared with the export
// pipeline; re-exported here so LayerManager's own lib API stays stable.
export type { Layer, LegendType, CategoricalStop, CogData } from '../../_shared/legend/types'
