[
  {
    "pr": 80,
    "thread_id": "inline-3191581497",
    "source": "inline",
    "resolved": false,
    "outdated": true,
    "path": "src/essence/Basics/UserInterface_/UserInterfaceModern_.js",
    "line": 29,
    "commit": "e266fe7c85e01f8888065475d72db7f1253b241f",
    "diff_hunk": "@@ -1,48 +1,205 @@\n import $ from 'jquery'\n import PanelManager_ from '../PanelManager_/PanelManager_'\n+import ToolControllerModern_ from '../ToolController_/ToolControllerModern_'\n+import { validateToolMetadata } from '../ToolController_/ToolMetadataUtils'\n+import { createLogger } from '../Logger_/Logger_'\n import './UserInterfaceModern_.css'\n \n+const logger = createLogger('UserInterfaceModern')\n+\n const DEFAULT_MIN_PANEL_SIZE = 50;\n const DEFAULT_MAX_PANEL_SIZE = 9999;\n \n+// --- Module-Level State ---\n+let panels = []\n+let layoutStyle = ''\n+let toolLoadQueue = []\n+let boundSyncDOMState = null\n+let layoutEventListenerAdded = false\n+\n+// --- DOM Builder Helpers ---\n+const _createPanelIconTray = (panel) => {\n+    const iconsContainer = $('<div class=\"ui-panel-icons\"></div>')\n+    const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n+\n+    if (toolsMetadata.length > 0) {\n+        toolsMetadata.forEach(toolMetadata => {\n+            try {\n+                validateToolMetadata(toolMetadata)\n+                const toolName = toolMetadata.name || toolMetadata.id",
    "url": "https://github.com/NASA-IMPACT/MMGIS/pull/80#discussion_r3191581497",
    "comments": [
      {
        "author": "slesaad",
        "body": "sanitize `toolName` and `toolId` so prevent XSS",
        "createdAt": "2026-05-05T21:08:28Z"
      }
    ],
    "code_context": "full",
    "code_window": "     1  import $ from 'jquery'\n     2  import PanelManager_ from '../PanelManager_/PanelManager_'\n     3  import ToolControllerModern_ from '../ToolController_/ToolControllerModern_'\n     4  import { validateToolMetadata } from '../ToolController_/ToolMetadataUtils'\n     5  import { createLogger } from '../Logger_/Logger_'\n     6  import './UserInterfaceModern_.css'\n     7  \n     8  const logger = createLogger('UserInterfaceModern')\n     9  \n    10  const DEFAULT_MIN_PANEL_SIZE = 50;\n    11  const DEFAULT_MAX_PANEL_SIZE = 9999;\n    12  \n    13  // --- Module-Level State ---\n    14  let panels = []\n    15  let layoutStyle = ''\n    16  let toolLoadQueue = []\n    17  let boundSyncDOMState = null\n    18  let layoutEventListenerAdded = false\n    19  \n    20  // --- DOM Builder Helpers ---\n    21  const _createPanelIconTray = (panel) => {\n    22      const iconsContainer = $('<div class=\"ui-panel-icons\"></div>')\n    23      const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n    24  \n    25      if (toolsMetadata.length > 0) {\n    26          toolsMetadata.forEach(toolMetadata => {\n    27              try {\n    28                  validateToolMetadata(toolMetadata)\n    29                  const toolName = toolMetadata.name || toolMetadata.id\n    30                  const toolId = toolMetadata.id\n    31                  const iconClass = toolMetadata.icon\n    32                  const iconBtn = $(`<button class=\"ui-panel-icon-btn\" title=\"${toolName}\" data-tool=\"${toolId}\"><span class=\"${iconClass}\"></span></button>`)\n    33                  \n    34                  iconBtn.on('click', () => {\n    35                      if (panel.state === 'focused' && panel.activeToolId === toolId) {\n    36                          PanelManager_.setPanelState(panel.id, 'iconified')\n    37                      } else {\n    38                          PanelManager_.focusTool(panel.id, toolId)\n    39                      }\n    40                  })\n    41                  iconsContainer.append(iconBtn)\n    42              } catch (error) {\n    43                  logger.error(`Failed to create icon button for tool in panel \"${panel.id}\":`, error)\n    44              }\n    45          })\n    46      }\n    47      return iconsContainer\n    48  }\n    49  \n    50  const _createPanelHeader = (panel) => {\n    51      const allowed = panel.config.stateConstraints?.allowedStates || []\n    52      const header = $('<div class=\"ui-panel-header\"></div>')\n    53      const title = $('<h3 class=\"ui-panel-title\"></h3>').text(panel.config.title || panel.id)\n    54      const headerButtons = $('<div class=\"ui-panel-header-buttons\"></div>')\n    55  \n    56      if (allowed.includes('iconified') || allowed.includes('collapsed')) {\n    57          const minBtn = $('<button class=\"ui-panel-btn ui-panel-btn-minimize\" title=\"Minimize\"><span class=\"mdi mdi-window-minimize\"></span></button>')\n    58          minBtn.on('click', () => {\n    59              const target = allowed.includes('iconified') ? 'iconified' : 'collapsed'\n    60              PanelManager_.setPanelState(panel.id, target)\n    61          })\n    62          headerButtons.append(minBtn)\n    63      }\n    64  \n    65      if (allowed.includes('expanded')) {\n    66          const maxBtn = $('<button class=\"ui-panel-btn ui-panel-btn-maximize\" title=\"Maximize\"><span class=\"mdi mdi-window-maximize\"></span></button>')\n    67          maxBtn.on('click', () => {\n    68              PanelManager_.setPanelState(panel.id, 'expanded')\n    69          })\n    70          headerButtons.append(maxBtn)\n    71      }\n    72  \n    73      if (allowed.includes('collapsed')) {\n    74          const closeBtn = $('<button class=\"ui-panel-btn ui-panel-btn-close\" title=\"Close\"><span class=\"mdi mdi-close\"></span></button>')\n    75          closeBtn.on('click', () => {\n    76              PanelManager_.setPanelState(panel.id, 'collapsed')\n    77          })\n    78          headerButtons.append(closeBtn)\n    79      }",
    "post_comment_delta": "diff --git a/src/essence/Basics/UserInterface_/UserInterfaceModern_.js b/src/essence/Basics/UserInterface_/UserInterfaceModern_.js\nindex cff522aa..78ae81ff 100644\n--- a/src/essence/Basics/UserInterface_/UserInterfaceModern_.js\n+++ b/src/essence/Basics/UserInterface_/UserInterfaceModern_.js\n@@ -1,7 +1,8 @@\n import $ from 'jquery'\n import PanelManager_ from '../PanelManager_/PanelManager_'\n+import { mmgisAPI } from '../../mmgisAPI/mmgisAPI'\n import ToolControllerModern_ from '../ToolController_/ToolControllerModern_'\n-import { validateToolMetadata } from '../ToolController_/ToolMetadataUtils'\n+import { getValidIconClass } from '../ToolController_/ToolMetadataUtils'\n import { createLogger } from '../Logger_/Logger_'\n import './UserInterfaceModern_.css'\n \n@@ -14,36 +15,36 @@ const DEFAULT_MAX_PANEL_SIZE = 9999;\n let panels = []\n let layoutStyle = ''\n let toolLoadQueue = []\n-let boundSyncDOMState = null\n-let layoutEventListenerAdded = false\n+let cleanupLayoutListener = null\n+let _resizeObserver = null\n \n // --- DOM Builder Helpers ---\n const _createPanelIconTray = (panel) => {\n     const iconsContainer = $('<div class=\"ui-panel-icons\"></div>')\n     const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n \n-    if (toolsMetadata.length > 0) {\n-        toolsMetadata.forEach(toolMetadata => {\n-            try {\n-                validateToolMetadata(toolMetadata)\n-                const toolName = toolMetadata.name || toolMetadata.id\n-                const toolId = toolMetadata.id\n-                const iconClass = toolMetadata.icon\n-                const iconBtn = $(`<button class=\"ui-panel-icon-btn\" title=\"${toolName}\" data-tool=\"${toolId}\"><span class=\"${iconClass}\"></span></button>`)\n-                \n-                iconBtn.on('click', () => {\n-                    if (panel.state === 'focused' && panel.activeToolId === toolId) {\n-                        PanelManager_.setPanelState(panel.id, 'iconified')\n-                    } else {\n-                        PanelManager_.focusTool(panel.id, toolId)\n-                    }\n-                })\n-                iconsContainer.append(iconBtn)\n-            } catch (error) {\n-                logger.error(`Failed to create icon button for tool in panel \"${panel.id}\":`, error)\n+    toolsMetadata.forEach(toolMetadata => {\n+        const toolName = toolMetadata.name || toolMetadata.id\n+        const toolId = toolMetadata.id\n+        const iconClass = getValidIconClass(toolMetadata.icon, toolId)\n+\n+        // Use safe jQuery methods to create elements and set attributes\n+        const iconSpan = $('<span></span>').addClass(iconClass)\n+        const iconBtn = $('<button></button>')\n+            .addClass('ui-panel-icon-btn')\n+            .attr('title', toolName) // jQuery escapes attribute values\n+            .attr('data-tool', toolId)\n+            .append(iconSpan)\n+\n+        iconBtn.on('click', () => {\n+            if (panel.state === 'focused' && panel.activeToolId === toolId) {\n+                PanelManager_.setPanelState(panel.id, 'iconified')\n+            } else {\n+                PanelManager_.focusTool(panel.id, toolId)\n             }\n         })\n-    }\n+        iconsContainer.append(iconBtn)\n+    })\n     return iconsContainer\n }\n \n@@ -140,10 +141,20 @@ const UserInterfaceModern_ = {\n         layoutStyle = style\n         this.render()\n \n-        if (!layoutEventListenerAdded) {\n-            boundSyncDOMState = this.syncDOMState.bind(this)\n-            window.addEventListener('mmgis-panel-layout-changed', boundSyncDOMState)\n-            layoutEventListenerAdded = true\n+        if (!cleanupLayoutListener) {\n+            cleanupLayoutListener = mmgisAPI.on('mmgis-panel-layout-changed', this.syncDOMState.bind(this))\n+        }\n+\n+        // Set up ResizeObserver to dispatch resize events when the center map area changes size\n+        if (_resizeObserver) {\n+            _resizeObserver.disconnect()\n+        }\n+        _resizeObserver = new ResizeObserver(() => {\n+            window.dispatchEvent(new Event('resize'))\n+        })\n+        const centerElement = document.getElementById('ui-modern-center')\n+        if (centerElement) {\n+            _resizeObserver.observe(centerElement)\n         }\n     },\n \n@@ -153,10 +164,13 @@ const UserInterfaceModern_ = {\n     destroy: function () {\n         ToolControllerModern_.destroyAllTools()\n         toolLoadQueue = [] // Clear any pending loads\n-        if (layoutEventListenerAdded && boundSyncDOMState) {\n-            window.removeEventListener('mmgis-panel-layout-changed', boundSyncDOMState)\n-            boundSyncDOMState = null\n-            layoutEventListenerAdded = false\n+        if (cleanupLayoutListener) {\n+            cleanupLayoutListener()\n+            cleanupLayoutListener = null\n+        }\n+        if (_resizeObserver) {\n+            _resizeObserver.disconnect()\n+            _resizeObserver = null\n         }\n         $('#modern-content').empty()\n         panels = []\n@@ -174,26 +188,29 @@ const UserInterfaceModern_ = {\n      * @throws {Error} If toolMetadata is invalid or panelId is missing\n      */\n     createToolCard: function (toolMetadata, panelId, layoutType = 'stacked', isActive = false) {\n-        validateToolMetadata(toolMetadata)\n-\n         if (!panelId) {\n             throw new Error('[UserInterfaceModern] panelId is required for createToolCard')\n         }\n \n         const toolId = toolMetadata.id\n         const targetId = `${panelId}-tool-${toolId}`\n-        const activeClass = isActive ? 'active' : ''\n         const layoutClass = layoutType === 'tabbed' ? 'ui-tool-tab-content' : 'ui-tool-card-stacked'\n \n-        const toolCard = $(`<div class=\"ui-tool-card ${layoutClass} ${activeClass}\" data-tool=\"${toolId}\" id=\"${targetId}\"></div>`)\n+        const toolCard = $('<div></div>')\n+            .addClass('ui-tool-card')\n+            .addClass(layoutClass)\n+            .addClass(isActive ? 'active' : '')\n+            .attr('data-tool', toolId)\n+            .attr('id', targetId)\n \n         // Return card and a function to load the tool (called after append)\n         // The load function checks if DOM element still exists before loading\n         return {\n             toolCard: toolCard,\n             loadTool: () => {\n-                const $target = $(`#${targetId}`)\n-                if ($target.length === 0) {\n+\n+                const targetElement = document.getElementById(targetId)\n+                if (!targetElement) {\n                     logger.warn(`Target element \"${targetId}\" not found in DOM, skipping tool load`)\n                     return\n                 }\n@@ -218,37 +235,31 @@ const UserInterfaceModern_ = {\n         const tabBar = $('<div class=\"ui-panel-tabs\"></div>')\n         const tabContentArea = $('<div class=\"ui-panel-tab-content-area\"></div>')\n \n-        // Get tool metadata from PanelManager\n-        let toolsMetadata = []\n-        try {\n-            toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n-        } catch (error) {\n-            logger.error(`Failed to get tools for panel \"${panel.id}\":`, error)\n-            return\n-        }\n+        const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n \n         toolsMetadata.forEach((toolMetadata, idx) => {\n-            try {\n-                validateToolMetadata(toolMetadata)\n-\n-                const toolName = toolMetadata.name || toolMetadata.id\n-                const toolId = toolMetadata.id\n-                const isActive = idx === 0\n-\n-                // Create tab\n-                const iconClass = toolMetadata.icon\n-                const tab = $(`<div class=\"ui-panel-tab ${isActive ? 'active' : ''}\" data-tool=\"${toolId}\"></div>`)\n-                    .append(`<span class=\"ui-tool-icon ${iconClass}\"></span>`)\n-                    .append($('<span></span>').text(toolName))\n-                tabBar.append(tab)\n-\n-                // Create tab content container and queue tool loading\n-                const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId, 'tabbed', isActive)\n-                tabContentArea.append(toolCard)\n-                toolLoadQueue.push(loadTool)\n-            } catch (error) {\n-                logger.error(`Failed to render tool in tabbed layout (panel: ${panel.id}):`, error)\n-            }\n+            const toolName = toolMetadata.name || toolMetadata.id\n+            const toolId = toolMetadata.id\n+            const isActive = idx === 0\n+\n+            // Create tab using safe jQuery methods\n+            const iconClass = getValidIconClass(toolMetadata.icon, toolId)\n+            const iconSpan = $('<span></span>').addClass('ui-tool-icon').addClass(iconClass)\n+            const textSpan = $('<span></span>').text(toolName) // .text() is safe - auto-escapes\n+\n+            const tab = $('<div></div>')\n+                .addClass('ui-panel-tab')\n+                .addClass(isActive ? 'active' : '')\n+                .attr('data-tool', toolId)\n+                .append(iconSpan)\n+                .append(textSpan)\n+\n+            tabBar.append(tab)\n+\n+            // Create tab content container and queue tool loading\n+            const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId, 'tabbed', isActive)\n+            tabContentArea.append(toolCard)\n+            toolLoadQueue.push(loadTool)\n         })\n \n         body.append(tabBar).append(tabContentArea)\n@@ -264,7 +275,9 @@ const UserInterfaceModern_ = {\n \n             // Update active content\n             tabContentArea.find('.ui-tool-tab-content').removeClass('active')\n-            tabContentArea.find(`.ui-tool-tab-content[data-tool=\"${targetTool}\"]`).addClass('active')\n+            tabContentArea.find('.ui-tool-tab-content').filter((_, element) => {\n+                return $(element).data('tool') === targetTool\n+            }).addClass('active')\n         })\n     },\n \n@@ -279,25 +292,13 @@ const UserInterfaceModern_ = {\n             return\n         }\n \n-        // Get tool metadata from PanelManager\n-        let toolsMetadata = []\n-        try {\n-            toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n-        } catch (error) {\n-            logger.error(`Failed to get tools for panel \"${panel.id}\":`, error)\n-            return\n-        }\n+        const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n \n         toolsMetadata.forEach(toolMetadata => {\n-            try {\n-                const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId)\n-                body.append(toolCard)\n-                // Queue tool loading for after DOM is fully rendered\n-                toolLoadQueue.push(loadTool)\n-            } catch (error) {\n-                logger.error(`Failed to render tool in stacked layout (panel: ${panel.id}):`, error)\n-                // Skip this tool and continue with others\n-            }\n+            const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId)\n+            body.append(toolCard)\n+            // Queue tool loading for after DOM is fully rendered\n+            toolLoadQueue.push(loadTool)\n         })\n     },\n \n@@ -429,11 +430,11 @@ const UserInterfaceModern_ = {\n \n     /**\n      * Passively synchronize DOM based on event payload without destroying nodes.\n-     * @param {CustomEvent} e Custom layout changed event fired from PanelManager\n+     * @param {Object} e Layout changed event payload\n      */\n     syncDOMState: function (e) {\n-        if (e && e.detail && e.detail.panels) {\n-            panels = e.detail.panels\n+        if (e && e.panels) {\n+            this.panels = e.panels\n         }\n \n         panels.forEach(panel => {\n@@ -444,12 +445,17 @@ const UserInterfaceModern_ = {\n \n             if (panel.activeToolId && panel.state === 'focused') {\n                 // Update active state on icons\n+                const activeToolId = panel.activeToolId\n                 $panel.find('.ui-panel-icon-btn').removeClass('active')\n-                $panel.find(`.ui-panel-icon-btn[data-tool=\"${panel.activeToolId}\"]`).addClass('active')\n+                $panel.find('.ui-panel-icon-btn').filter((_, element) => {\n+                    return $(element).data('tool') === activeToolId\n+                }).addClass('active')\n \n                 // Update active state on tool cards (especially for focused state and tabbed)\n                 $panel.find('.ui-tool-card').removeClass('active')\n-                $panel.find(`.ui-tool-card[data-tool=\"${panel.activeToolId}\"]`).addClass('active')\n+                $panel.find('.ui-tool-card').filter((_, element) => {\n+                    return $(element).data('tool') === activeToolId\n+                }).addClass('active')\n             } else {\n                 // Remove active highlighting when not focused\n                 $panel.find('.ui-panel-icon-btn').removeClass('active')\n@@ -476,13 +482,6 @@ const UserInterfaceModern_ = {\n                 }\n             }\n         })\n-\n-        if (layoutStyle === 'compact') {\n-            window.dispatchEvent(new Event('resize'))\n-            setTimeout(() => {\n-                window.dispatchEvent(new Event('resize'))\n-            }, 300)\n-        }\n     },\n \n     /**",
    "pr_commits_after": [
      "3842850f  Improve metadata validation and add sanitization to prevent XSS attacks",
      "71a365a5  Add dompurify package for sanitization",
      "9aea9038  Merge branch 'epic/modern-ui' into feat/render-tools",
      "268b2d41  Refactor title tool as a react component",
      "549dc9a0  Rename loadAndAssignTools to assignToolsToPanels for clarity",
      "0b3570e1  Update event handler for layout change",
      "7b96fec0  Refactor cleanup process in ModernInterface to include UserInterfaceModern destruction",
      "8125e757  Add ResizeObserver to handle dynamic resizing of the center map area",
      "883044fc  Improve duplicate tool handling in buildToolConfigMap with clearer logging"
    ]
  },
  {
    "pr": 80,
    "thread_id": "inline-3191585186",
    "source": "inline",
    "resolved": false,
    "outdated": false,
    "path": "src/essence/Basics/UserInterface_/UserInterfaceModern_.js",
    "line": 53,
    "commit": "e266fe7c85e01f8888065475d72db7f1253b241f",
    "diff_hunk": "@@ -1,48 +1,205 @@\n import $ from 'jquery'\n import PanelManager_ from '../PanelManager_/PanelManager_'\n+import ToolControllerModern_ from '../ToolController_/ToolControllerModern_'\n+import { validateToolMetadata } from '../ToolController_/ToolMetadataUtils'\n+import { createLogger } from '../Logger_/Logger_'\n import './UserInterfaceModern_.css'\n \n+const logger = createLogger('UserInterfaceModern')\n+\n const DEFAULT_MIN_PANEL_SIZE = 50;\n const DEFAULT_MAX_PANEL_SIZE = 9999;\n \n+// --- Module-Level State ---\n+let panels = []\n+let layoutStyle = ''\n+let toolLoadQueue = []\n+let boundSyncDOMState = null\n+let layoutEventListenerAdded = false\n+\n+// --- DOM Builder Helpers ---\n+const _createPanelIconTray = (panel) => {\n+    const iconsContainer = $('<div class=\"ui-panel-icons\"></div>')\n+    const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n+\n+    if (toolsMetadata.length > 0) {\n+        toolsMetadata.forEach(toolMetadata => {\n+            try {\n+                validateToolMetadata(toolMetadata)\n+                const toolName = toolMetadata.name || toolMetadata.id\n+                const toolId = toolMetadata.id\n+                const iconClass = toolMetadata.icon\n+                const iconBtn = $(`<button class=\"ui-panel-icon-btn\" title=\"${toolName}\" data-tool=\"${toolId}\"><span class=\"${iconClass}\"></span></button>`)\n+                \n+                iconBtn.on('click', () => {\n+                    if (panel.state === 'focused' && panel.activeToolId === toolId) {\n+                        PanelManager_.setPanelState(panel.id, 'iconified')\n+                    } else {\n+                        PanelManager_.focusTool(panel.id, toolId)\n+                    }\n+                })\n+                iconsContainer.append(iconBtn)\n+            } catch (error) {\n+                logger.error(`Failed to create icon button for tool in panel \"${panel.id}\":`, error)\n+            }\n+        })\n+    }\n+    return iconsContainer\n+}\n+\n+const _createPanelHeader = (panel) => {\n+    const allowed = panel.config.stateConstraints?.allowedStates || []\n+    const header = $('<div class=\"ui-panel-header\"></div>')\n+    const title = $('<h3 class=\"ui-panel-title\"></h3>').text(panel.config.title || panel.id)",
    "url": "https://github.com/NASA-IMPACT/MMGIS/pull/80#discussion_r3191585186",
    "comments": [
      {
        "author": "slesaad",
        "body": "sanitize title to prevent xss",
        "createdAt": "2026-05-05T21:09:18Z"
      }
    ],
    "code_context": "full",
    "code_window": "     3  import ToolControllerModern_ from '../ToolController_/ToolControllerModern_'\n     4  import { validateToolMetadata } from '../ToolController_/ToolMetadataUtils'\n     5  import { createLogger } from '../Logger_/Logger_'\n     6  import './UserInterfaceModern_.css'\n     7  \n     8  const logger = createLogger('UserInterfaceModern')\n     9  \n    10  const DEFAULT_MIN_PANEL_SIZE = 50;\n    11  const DEFAULT_MAX_PANEL_SIZE = 9999;\n    12  \n    13  // --- Module-Level State ---\n    14  let panels = []\n    15  let layoutStyle = ''\n    16  let toolLoadQueue = []\n    17  let boundSyncDOMState = null\n    18  let layoutEventListenerAdded = false\n    19  \n    20  // --- DOM Builder Helpers ---\n    21  const _createPanelIconTray = (panel) => {\n    22      const iconsContainer = $('<div class=\"ui-panel-icons\"></div>')\n    23      const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n    24  \n    25      if (toolsMetadata.length > 0) {\n    26          toolsMetadata.forEach(toolMetadata => {\n    27              try {\n    28                  validateToolMetadata(toolMetadata)\n    29                  const toolName = toolMetadata.name || toolMetadata.id\n    30                  const toolId = toolMetadata.id\n    31                  const iconClass = toolMetadata.icon\n    32                  const iconBtn = $(`<button class=\"ui-panel-icon-btn\" title=\"${toolName}\" data-tool=\"${toolId}\"><span class=\"${iconClass}\"></span></button>`)\n    33                  \n    34                  iconBtn.on('click', () => {\n    35                      if (panel.state === 'focused' && panel.activeToolId === toolId) {\n    36                          PanelManager_.setPanelState(panel.id, 'iconified')\n    37                      } else {\n    38                          PanelManager_.focusTool(panel.id, toolId)\n    39                      }\n    40                  })\n    41                  iconsContainer.append(iconBtn)\n    42              } catch (error) {\n    43                  logger.error(`Failed to create icon button for tool in panel \"${panel.id}\":`, error)\n    44              }\n    45          })\n    46      }\n    47      return iconsContainer\n    48  }\n    49  \n    50  const _createPanelHeader = (panel) => {\n    51      const allowed = panel.config.stateConstraints?.allowedStates || []\n    52      const header = $('<div class=\"ui-panel-header\"></div>')\n    53      const title = $('<h3 class=\"ui-panel-title\"></h3>').text(panel.config.title || panel.id)\n    54      const headerButtons = $('<div class=\"ui-panel-header-buttons\"></div>')\n    55  \n    56      if (allowed.includes('iconified') || allowed.includes('collapsed')) {\n    57          const minBtn = $('<button class=\"ui-panel-btn ui-panel-btn-minimize\" title=\"Minimize\"><span class=\"mdi mdi-window-minimize\"></span></button>')\n    58          minBtn.on('click', () => {\n    59              const target = allowed.includes('iconified') ? 'iconified' : 'collapsed'\n    60              PanelManager_.setPanelState(panel.id, target)\n    61          })\n    62          headerButtons.append(minBtn)\n    63      }\n    64  \n    65      if (allowed.includes('expanded')) {\n    66          const maxBtn = $('<button class=\"ui-panel-btn ui-panel-btn-maximize\" title=\"Maximize\"><span class=\"mdi mdi-window-maximize\"></span></button>')\n    67          maxBtn.on('click', () => {\n    68              PanelManager_.setPanelState(panel.id, 'expanded')\n    69          })\n    70          headerButtons.append(maxBtn)\n    71      }\n    72  \n    73      if (allowed.includes('collapsed')) {\n    74          const closeBtn = $('<button class=\"ui-panel-btn ui-panel-btn-close\" title=\"Close\"><span class=\"mdi mdi-close\"></span></button>')\n    75          closeBtn.on('click', () => {\n    76              PanelManager_.setPanelState(panel.id, 'collapsed')\n    77          })\n    78          headerButtons.append(closeBtn)\n    79      }\n    80  \n    81      header.append(title).append(headerButtons)\n    82      return header\n    83  }\n    84  \n    85  const _renderRegion = (regionName, regionPanels) => {\n    86      if (!regionPanels || regionPanels.length === 0) return null\n    87      \n    88      const regionDiv = $(`<div class=\"ui-region ui-region-${regionName}\"></div>`)\n    89      \n    90      regionPanels.forEach(panel => {\n    91          const panelDiv = $('<div class=\"ui-panel\"></div>').attr('id', panel.containerId)\n    92          const contentWrapper = $('<div class=\"ui-panel-content\"></div>')\n    93          const body = $('<div class=\"ui-panel-body\"></div>')\n    94  \n    95          // Icon Tray\n    96          panelDiv.append(_createPanelIconTray(panel))\n    97  \n    98          // Header\n    99          if (panel.config.hasHeader) {\n   100              contentWrapper.append(_createPanelHeader(panel))\n   101          }\n   102  \n   103          panelDiv.attr('data-panel-state', panel.state)",
    "post_comment_delta": "diff --git a/src/essence/Basics/UserInterface_/UserInterfaceModern_.js b/src/essence/Basics/UserInterface_/UserInterfaceModern_.js\nindex cff522aa..78ae81ff 100644\n--- a/src/essence/Basics/UserInterface_/UserInterfaceModern_.js\n+++ b/src/essence/Basics/UserInterface_/UserInterfaceModern_.js\n@@ -1,7 +1,8 @@\n import $ from 'jquery'\n import PanelManager_ from '../PanelManager_/PanelManager_'\n+import { mmgisAPI } from '../../mmgisAPI/mmgisAPI'\n import ToolControllerModern_ from '../ToolController_/ToolControllerModern_'\n-import { validateToolMetadata } from '../ToolController_/ToolMetadataUtils'\n+import { getValidIconClass } from '../ToolController_/ToolMetadataUtils'\n import { createLogger } from '../Logger_/Logger_'\n import './UserInterfaceModern_.css'\n \n@@ -14,36 +15,36 @@ const DEFAULT_MAX_PANEL_SIZE = 9999;\n let panels = []\n let layoutStyle = ''\n let toolLoadQueue = []\n-let boundSyncDOMState = null\n-let layoutEventListenerAdded = false\n+let cleanupLayoutListener = null\n+let _resizeObserver = null\n \n // --- DOM Builder Helpers ---\n const _createPanelIconTray = (panel) => {\n     const iconsContainer = $('<div class=\"ui-panel-icons\"></div>')\n     const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n \n-    if (toolsMetadata.length > 0) {\n-        toolsMetadata.forEach(toolMetadata => {\n-            try {\n-                validateToolMetadata(toolMetadata)\n-                const toolName = toolMetadata.name || toolMetadata.id\n-                const toolId = toolMetadata.id\n-                const iconClass = toolMetadata.icon\n-                const iconBtn = $(`<button class=\"ui-panel-icon-btn\" title=\"${toolName}\" data-tool=\"${toolId}\"><span class=\"${iconClass}\"></span></button>`)\n-                \n-                iconBtn.on('click', () => {\n-                    if (panel.state === 'focused' && panel.activeToolId === toolId) {\n-                        PanelManager_.setPanelState(panel.id, 'iconified')\n-                    } else {\n-                        PanelManager_.focusTool(panel.id, toolId)\n-                    }\n-                })\n-                iconsContainer.append(iconBtn)\n-            } catch (error) {\n-                logger.error(`Failed to create icon button for tool in panel \"${panel.id}\":`, error)\n+    toolsMetadata.forEach(toolMetadata => {\n+        const toolName = toolMetadata.name || toolMetadata.id\n+        const toolId = toolMetadata.id\n+        const iconClass = getValidIconClass(toolMetadata.icon, toolId)\n+\n+        // Use safe jQuery methods to create elements and set attributes\n+        const iconSpan = $('<span></span>').addClass(iconClass)\n+        const iconBtn = $('<button></button>')\n+            .addClass('ui-panel-icon-btn')\n+            .attr('title', toolName) // jQuery escapes attribute values\n+            .attr('data-tool', toolId)\n+            .append(iconSpan)\n+\n+        iconBtn.on('click', () => {\n+            if (panel.state === 'focused' && panel.activeToolId === toolId) {\n+                PanelManager_.setPanelState(panel.id, 'iconified')\n+            } else {\n+                PanelManager_.focusTool(panel.id, toolId)\n             }\n         })\n-    }\n+        iconsContainer.append(iconBtn)\n+    })\n     return iconsContainer\n }\n \n@@ -140,10 +141,20 @@ const UserInterfaceModern_ = {\n         layoutStyle = style\n         this.render()\n \n-        if (!layoutEventListenerAdded) {\n-            boundSyncDOMState = this.syncDOMState.bind(this)\n-            window.addEventListener('mmgis-panel-layout-changed', boundSyncDOMState)\n-            layoutEventListenerAdded = true\n+        if (!cleanupLayoutListener) {\n+            cleanupLayoutListener = mmgisAPI.on('mmgis-panel-layout-changed', this.syncDOMState.bind(this))\n+        }\n+\n+        // Set up ResizeObserver to dispatch resize events when the center map area changes size\n+        if (_resizeObserver) {\n+            _resizeObserver.disconnect()\n+        }\n+        _resizeObserver = new ResizeObserver(() => {\n+            window.dispatchEvent(new Event('resize'))\n+        })\n+        const centerElement = document.getElementById('ui-modern-center')\n+        if (centerElement) {\n+            _resizeObserver.observe(centerElement)\n         }\n     },\n \n@@ -153,10 +164,13 @@ const UserInterfaceModern_ = {\n     destroy: function () {\n         ToolControllerModern_.destroyAllTools()\n         toolLoadQueue = [] // Clear any pending loads\n-        if (layoutEventListenerAdded && boundSyncDOMState) {\n-            window.removeEventListener('mmgis-panel-layout-changed', boundSyncDOMState)\n-            boundSyncDOMState = null\n-            layoutEventListenerAdded = false\n+        if (cleanupLayoutListener) {\n+            cleanupLayoutListener()\n+            cleanupLayoutListener = null\n+        }\n+        if (_resizeObserver) {\n+            _resizeObserver.disconnect()\n+            _resizeObserver = null\n         }\n         $('#modern-content').empty()\n         panels = []\n@@ -174,26 +188,29 @@ const UserInterfaceModern_ = {\n      * @throws {Error} If toolMetadata is invalid or panelId is missing\n      */\n     createToolCard: function (toolMetadata, panelId, layoutType = 'stacked', isActive = false) {\n-        validateToolMetadata(toolMetadata)\n-\n         if (!panelId) {\n             throw new Error('[UserInterfaceModern] panelId is required for createToolCard')\n         }\n \n         const toolId = toolMetadata.id\n         const targetId = `${panelId}-tool-${toolId}`\n-        const activeClass = isActive ? 'active' : ''\n         const layoutClass = layoutType === 'tabbed' ? 'ui-tool-tab-content' : 'ui-tool-card-stacked'\n \n-        const toolCard = $(`<div class=\"ui-tool-card ${layoutClass} ${activeClass}\" data-tool=\"${toolId}\" id=\"${targetId}\"></div>`)\n+        const toolCard = $('<div></div>')\n+            .addClass('ui-tool-card')\n+            .addClass(layoutClass)\n+            .addClass(isActive ? 'active' : '')\n+            .attr('data-tool', toolId)\n+            .attr('id', targetId)\n \n         // Return card and a function to load the tool (called after append)\n         // The load function checks if DOM element still exists before loading\n         return {\n             toolCard: toolCard,\n             loadTool: () => {\n-                const $target = $(`#${targetId}`)\n-                if ($target.length === 0) {\n+\n+                const targetElement = document.getElementById(targetId)\n+                if (!targetElement) {\n                     logger.warn(`Target element \"${targetId}\" not found in DOM, skipping tool load`)\n                     return\n                 }\n@@ -218,37 +235,31 @@ const UserInterfaceModern_ = {\n         const tabBar = $('<div class=\"ui-panel-tabs\"></div>')\n         const tabContentArea = $('<div class=\"ui-panel-tab-content-area\"></div>')\n \n-        // Get tool metadata from PanelManager\n-        let toolsMetadata = []\n-        try {\n-            toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n-        } catch (error) {\n-            logger.error(`Failed to get tools for panel \"${panel.id}\":`, error)\n-            return\n-        }\n+        const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n \n         toolsMetadata.forEach((toolMetadata, idx) => {\n-            try {\n-                validateToolMetadata(toolMetadata)\n-\n-                const toolName = toolMetadata.name || toolMetadata.id\n-                const toolId = toolMetadata.id\n-                const isActive = idx === 0\n-\n-                // Create tab\n-                const iconClass = toolMetadata.icon\n-                const tab = $(`<div class=\"ui-panel-tab ${isActive ? 'active' : ''}\" data-tool=\"${toolId}\"></div>`)\n-                    .append(`<span class=\"ui-tool-icon ${iconClass}\"></span>`)\n-                    .append($('<span></span>').text(toolName))\n-                tabBar.append(tab)\n-\n-                // Create tab content container and queue tool loading\n-                const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId, 'tabbed', isActive)\n-                tabContentArea.append(toolCard)\n-                toolLoadQueue.push(loadTool)\n-            } catch (error) {\n-                logger.error(`Failed to render tool in tabbed layout (panel: ${panel.id}):`, error)\n-            }\n+            const toolName = toolMetadata.name || toolMetadata.id\n+            const toolId = toolMetadata.id\n+            const isActive = idx === 0\n+\n+            // Create tab using safe jQuery methods\n+            const iconClass = getValidIconClass(toolMetadata.icon, toolId)\n+            const iconSpan = $('<span></span>').addClass('ui-tool-icon').addClass(iconClass)\n+            const textSpan = $('<span></span>').text(toolName) // .text() is safe - auto-escapes\n+\n+            const tab = $('<div></div>')\n+                .addClass('ui-panel-tab')\n+                .addClass(isActive ? 'active' : '')\n+                .attr('data-tool', toolId)\n+                .append(iconSpan)\n+                .append(textSpan)\n+\n+            tabBar.append(tab)\n+\n+            // Create tab content container and queue tool loading\n+            const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId, 'tabbed', isActive)\n+            tabContentArea.append(toolCard)\n+            toolLoadQueue.push(loadTool)\n         })\n \n         body.append(tabBar).append(tabContentArea)\n@@ -264,7 +275,9 @@ const UserInterfaceModern_ = {\n \n             // Update active content\n             tabContentArea.find('.ui-tool-tab-content').removeClass('active')\n-            tabContentArea.find(`.ui-tool-tab-content[data-tool=\"${targetTool}\"]`).addClass('active')\n+            tabContentArea.find('.ui-tool-tab-content').filter((_, element) => {\n+                return $(element).data('tool') === targetTool\n+            }).addClass('active')\n         })\n     },\n \n@@ -279,25 +292,13 @@ const UserInterfaceModern_ = {\n             return\n         }\n \n-        // Get tool metadata from PanelManager\n-        let toolsMetadata = []\n-        try {\n-            toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n-        } catch (error) {\n-            logger.error(`Failed to get tools for panel \"${panel.id}\":`, error)\n-            return\n-        }\n+        const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n \n         toolsMetadata.forEach(toolMetadata => {\n-            try {\n-                const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId)\n-                body.append(toolCard)\n-                // Queue tool loading for after DOM is fully rendered\n-                toolLoadQueue.push(loadTool)\n-            } catch (error) {\n-                logger.error(`Failed to render tool in stacked layout (panel: ${panel.id}):`, error)\n-                // Skip this tool and continue with others\n-            }\n+            const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId)\n+            body.append(toolCard)\n+            // Queue tool loading for after DOM is fully rendered\n+            toolLoadQueue.push(loadTool)\n         })\n     },\n \n@@ -429,11 +430,11 @@ const UserInterfaceModern_ = {\n \n     /**\n      * Passively synchronize DOM based on event payload without destroying nodes.\n-     * @param {CustomEvent} e Custom layout changed event fired from PanelManager\n+     * @param {Object} e Layout changed event payload\n      */\n     syncDOMState: function (e) {\n-        if (e && e.detail && e.detail.panels) {\n-            panels = e.detail.panels\n+        if (e && e.panels) {\n+            this.panels = e.panels\n         }\n \n         panels.forEach(panel => {\n@@ -444,12 +445,17 @@ const UserInterfaceModern_ = {\n \n             if (panel.activeToolId && panel.state === 'focused') {\n                 // Update active state on icons\n+                const activeToolId = panel.activeToolId\n                 $panel.find('.ui-panel-icon-btn').removeClass('active')\n-                $panel.find(`.ui-panel-icon-btn[data-tool=\"${panel.activeToolId}\"]`).addClass('active')\n+                $panel.find('.ui-panel-icon-btn').filter((_, element) => {\n+                    return $(element).data('tool') === activeToolId\n+                }).addClass('active')\n \n                 // Update active state on tool cards (especially for focused state and tabbed)\n                 $panel.find('.ui-tool-card').removeClass('active')\n-                $panel.find(`.ui-tool-card[data-tool=\"${panel.activeToolId}\"]`).addClass('active')\n+                $panel.find('.ui-tool-card').filter((_, element) => {\n+                    return $(element).data('tool') === activeToolId\n+                }).addClass('active')\n             } else {\n                 // Remove active highlighting when not focused\n                 $panel.find('.ui-panel-icon-btn').removeClass('active')\n@@ -476,13 +482,6 @@ const UserInterfaceModern_ = {\n                 }\n             }\n         })\n-\n-        if (layoutStyle === 'compact') {\n-            window.dispatchEvent(new Event('resize'))\n-            setTimeout(() => {\n-                window.dispatchEvent(new Event('resize'))\n-            }, 300)\n-        }\n     },\n \n     /**",
    "pr_commits_after": [
      "3842850f  Improve metadata validation and add sanitization to prevent XSS attacks",
      "71a365a5  Add dompurify package for sanitization",
      "9aea9038  Merge branch 'epic/modern-ui' into feat/render-tools",
      "268b2d41  Refactor title tool as a react component",
      "549dc9a0  Rename loadAndAssignTools to assignToolsToPanels for clarity",
      "0b3570e1  Update event handler for layout change",
      "7b96fec0  Refactor cleanup process in ModernInterface to include UserInterfaceModern destruction",
      "8125e757  Add ResizeObserver to handle dynamic resizing of the center map area",
      "883044fc  Improve duplicate tool handling in buildToolConfigMap with clearer logging"
    ]
  },
  {
    "pr": 80,
    "thread_id": "inline-3203167084",
    "source": "inline",
    "resolved": false,
    "outdated": false,
    "path": "src/essence/Tools/Title/TitleTool.js",
    "line": 1,
    "commit": "e266fe7c85e01f8888065475d72db7f1253b241f",
    "diff_hunk": "",
    "url": "https://github.com/NASA-IMPACT/MMGIS/pull/80#discussion_r3203167084",
    "comments": [
      {
        "author": "slesaad",
        "body": "since this is an example tool - i'd make a react component and wrap that ",
        "createdAt": "2026-05-07T16:46:33Z"
      }
    ],
    "code_context": "full",
    "code_window": "     1  // Title Tool\r\n     2  // Displays a logo and title text from dashboard configuration\r\n     3  import $ from 'jquery'\r\n     4  import F_ from '../../Basics/Formulae_/Formulae_'\r\n     5  import L_ from '../../Basics/Layers_/Layers_'\r\n     6  import './TitleTool.css'\r\n     7  \r\n     8  // Tool markup\r\n     9  const markup = [\r\n    10      `<div id='titleTool'>`,\r\n    11      `  <div class='titleTool-container'>`,\r\n    12      `    <div class='titleTool-logo'>`,\r\n    13      `      <!-- Logo will be inserted here if configured -->`,\r\n    14      `    </div>`,\r\n    15      `    <div class='titleTool-icon'>`,\r\n    16      `      <i class='mdi mdi-earth mdi-24px'></i>`,\r\n    17      `    </div>`,\r\n    18      `    <div class='titleTool-text'>`,\r\n    19      `      <span class='titleTool-title'></span>`,\r\n    20      `    </div>`,\r\n    21      `  </div>`,\r\n    22      `</div>`,\r\n    23  ].join('\\n')\r\n    24  \r\n    25  const TitleTool = {\r\n    26      height: 60,\r\n    27      width: 'full',\r\n    28      MMGISInterface: null,\r\n    29      targetId: null,\r\n    30      made: false,\r\n    31      titleText: '',\r\n    32      iconClass: 'mdi mdi-earth mdi-24px',\r\n    33      logoUrl: null,\r\n    34  \r\n    35      initialize: function () {\r\n    36  \r\n    37          // Get title from dashboard config\r\n    38          this.titleText =\r\n    39              L_.configData?.look?.pagename ||\r\n    40              L_.configData?.msv?.mission ||\r\n    41              window.mmgisglobal?.name ||\r\n    42              'MMGIS'\r\n    43  \r\n    44          // Get logo from mission config (logoUrl)\r\n    45          if (L_.configData?.look?.logourl) {\r\n    46              this.logoUrl = L_.configData.look.logourl\r\n    47          }\r\n    48  \r\n    49          // Get icon from tool variables if configured\r\n    50          const toolVars = L_.getToolVars('title')\r\n    51  \r",
    "post_comment_delta": "diff --git a/src/essence/Tools/Title/TitleTool.js b/src/essence/Tools/Title/TitleTool.js\nindex d5f09f45..42fc7fa3 100644\n--- a/src/essence/Tools/Title/TitleTool.js\n+++ b/src/essence/Tools/Title/TitleTool.js\n@@ -1,27 +1,12 @@\n // Title Tool\r\n // Displays a logo and title text from dashboard configuration\r\n import $ from 'jquery'\r\n-import F_ from '../../Basics/Formulae_/Formulae_'\r\n+import ReactDOM from 'react-dom'\r\n+import React from 'react'\r\n import L_ from '../../Basics/Layers_/Layers_'\r\n+import TitleToolComponent from './TitleToolComponent'\r\n import './TitleTool.css'\r\n \r\n-// Tool markup\r\n-const markup = [\r\n-    `<div id='titleTool'>`,\r\n-    `  <div class='titleTool-container'>`,\r\n-    `    <div class='titleTool-logo'>`,\r\n-    `      <!-- Logo will be inserted here if configured -->`,\r\n-    `    </div>`,\r\n-    `    <div class='titleTool-icon'>`,\r\n-    `      <i class='mdi mdi-earth mdi-24px'></i>`,\r\n-    `    </div>`,\r\n-    `    <div class='titleTool-text'>`,\r\n-    `      <span class='titleTool-title'></span>`,\r\n-    `    </div>`,\r\n-    `  </div>`,\r\n-    `</div>`,\r\n-].join('\\n')\r\n-\r\n const TitleTool = {\r\n     height: 60,\r\n     width: 'full',\r\n@@ -76,42 +61,33 @@ function interfaceWithMMGIS() {\n         separateFromMMGIS()\r\n     }\r\n \r\n-    const tools = $(`#${TitleTool.targetId}`)\r\n-    tools.css('background', 'transparent')\r\n-    tools.empty()\r\n-\r\n-    // Add the markup to tools\r\n-    tools.html('<div style=\"height: 100%\">' + markup + '</div>')\r\n-\r\n-    // Set the title text\r\n-    $('#titleTool .titleTool-title').text(TitleTool.titleText)\r\n-\r\n-    // Add logo if configured\r\n-    if (TitleTool.logoUrl) {\r\n-        const logoImg = $('<img>')\r\n-            .attr('src', TitleTool.logoUrl)\r\n-            .attr('alt', 'Logo')\r\n-            .addClass('titleTool-logo-img')\r\n-        $('#titleTool .titleTool-logo').empty().append(logoImg)\r\n-        $('#titleTool .titleTool-logo').show()\r\n-        // Hide the icon when logo is present\r\n-        $('#titleTool .titleTool-icon').hide()\r\n-    } else {\r\n-        // Show icon when no logo is configured\r\n-        $('#titleTool .titleTool-logo').hide()\r\n-        $('#titleTool .titleTool-icon').show()\r\n+    const container = document.getElementById(TitleTool.targetId)\r\n+    if (!container) {\r\n+        console.error(\r\n+            `TitleTool: container ${TitleTool.targetId} not found`\r\n+        )\r\n+        return\r\n     }\r\n \r\n-    // Update icon if configured\r\n-    if (TitleTool.iconClass) {\r\n-        $('#titleTool .titleTool-icon i')\r\n-            .attr('class', TitleTool.iconClass)\r\n-    }\r\n+    // Set panel background to transparent for title tool\r\n+    $(container).css('background', 'transparent')\r\n+\r\n+    // Render React component\r\n+    ReactDOM.render(\r\n+        <TitleToolComponent\r\n+            titleText={TitleTool.titleText}\r\n+            logoUrl={TitleTool.logoUrl}\r\n+            iconClass={TitleTool.iconClass}\r\n+        />,\r\n+        container\r\n+    )\r\n \r\n     function separateFromMMGIS() {\r\n-        const tools = $(`#${TitleTool.targetId}`)\r\n-        tools.css('background', 'var(--color-k)')\r\n-        tools.empty()\r\n+        const container = document.getElementById(TitleTool.targetId)\r\n+        if (container) {\r\n+            ReactDOM.unmountComponentAtNode(container)\r\n+            $(container).css('background', 'var(--mmgis-panel-bg)')\r\n+        }\r\n     }\r\n }\r\n \r",
    "pr_commits_after": [
      "3842850f  Improve metadata validation and add sanitization to prevent XSS attacks",
      "71a365a5  Add dompurify package for sanitization",
      "9aea9038  Merge branch 'epic/modern-ui' into feat/render-tools",
      "268b2d41  Refactor title tool as a react component",
      "549dc9a0  Rename loadAndAssignTools to assignToolsToPanels for clarity",
      "0b3570e1  Update event handler for layout change",
      "7b96fec0  Refactor cleanup process in ModernInterface to include UserInterfaceModern destruction",
      "8125e757  Add ResizeObserver to handle dynamic resizing of the center map area",
      "883044fc  Improve duplicate tool handling in buildToolConfigMap with clearer logging"
    ]
  },
  {
    "pr": 80,
    "thread_id": "inline-3203171852",
    "source": "inline",
    "resolved": false,
    "outdated": false,
    "path": "src/essence/Tools/Title/config.json",
    "line": 1,
    "commit": "e266fe7c85e01f8888065475d72db7f1253b241f",
    "diff_hunk": "",
    "url": "https://github.com/NASA-IMPACT/MMGIS/pull/80#discussion_r3203171852",
    "comments": [
      {
        "author": "slesaad",
        "body": "since this is an example tool, i'd also add all the available keys eventhough it's using defaults",
        "createdAt": "2026-05-07T16:47:20Z"
      }
    ],
    "code_context": "full",
    "code_window": "     1  {\r\n     2      \"defaultIcon\": \"information-variant\",\r\n     3      \"description\": \"Display mission icon and title from the mission config file.\",\r\n     4      \"descriptionFull\": {\r\n     5          \"title\": \"Title Tool\",\r\n     6          \"example\": {\r\n     7              \"sortAlphabetically\": true\r\n     8          }\r\n     9      },\r\n    10      \"hasVars\": true,\r\n    11      \"name\": \"Title\",\r\n    12      \"toolbarPriority\": 3,\r\n    13      \"paths\": {\r\n    14          \"TitleTool\": \"essence/Tools/Title/TitleTool\"\r\n    15      }\r\n    16  }\r",
    "post_comment_delta": "diff --git a/src/essence/Tools/Title/config.json b/src/essence/Tools/Title/config.json\nindex 726bbdcc..0742490f 100644\n--- a/src/essence/Tools/Title/config.json\n+++ b/src/essence/Tools/Title/config.json\n@@ -1,5 +1,5 @@\n {\r\n-    \"defaultIcon\": \"information-variant\",\r\n+    \"name\": \"Title\",\r\n     \"description\": \"Display mission icon and title from the mission config file.\",\r\n     \"descriptionFull\": {\r\n         \"title\": \"Title Tool\",\r\n@@ -7,10 +7,27 @@\n             \"sortAlphabetically\": true\r\n         }\r\n     },\r\n+    \"defaultIcon\": \"information-variant\",\r\n     \"hasVars\": true,\r\n-    \"name\": \"Title\",\r\n     \"toolbarPriority\": 3,\r\n+    \"width\": 300,\r\n+    \"height\": 0,\r\n     \"paths\": {\r\n         \"TitleTool\": \"essence/Tools/Title/TitleTool\"\r\n+    },\r\n+    \"tests\": {},\r\n+    \"expandable\": false,\r\n+    \"separatedTool\": false,\r\n+    \"metadata\": {\r\n+        \"icon\": \"information-variant\",\r\n+        \"requiredOrientation\": \"horizontal\",\r\n+        \"compatiblePositions\": [\"top\"],\r\n+        \"preferredPosition\": \"top\",\r\n+        \"modernLayoutSupport\": true,\r\n+        \"width\": 300,\r\n+        \"height\": 0\r\n+    },\r\n+    \"config\": {\r\n+        \"rows\": []\r\n     }\r\n }\r",
    "pr_commits_after": [
      "3842850f  Improve metadata validation and add sanitization to prevent XSS attacks",
      "71a365a5  Add dompurify package for sanitization",
      "9aea9038  Merge branch 'epic/modern-ui' into feat/render-tools",
      "268b2d41  Refactor title tool as a react component",
      "549dc9a0  Rename loadAndAssignTools to assignToolsToPanels for clarity",
      "0b3570e1  Update event handler for layout change",
      "7b96fec0  Refactor cleanup process in ModernInterface to include UserInterfaceModern destruction",
      "8125e757  Add ResizeObserver to handle dynamic resizing of the center map area",
      "883044fc  Improve duplicate tool handling in buildToolConfigMap with clearer logging"
    ]
  },
  {
    "pr": 80,
    "thread_id": "inline-3203303435",
    "source": "inline",
    "resolved": false,
    "outdated": true,
    "path": "src/essence/Basics/UserInterface_/UserInterfaceModern_.js",
    "line": 484,
    "commit": "e266fe7c85e01f8888065475d72db7f1253b241f",
    "diff_hunk": "@@ -372,8 +477,11 @@ const UserInterfaceModern_ = {\n             }\n         })\n \n-        if (this.layoutStyle === 'compact') {\n+        if (layoutStyle === 'compact') {\n             window.dispatchEvent(new Event('resize'))\n+            setTimeout(() => {\n+                window.dispatchEvent(new Event('resize'))\n+            }, 300)",
    "url": "https://github.com/NASA-IMPACT/MMGIS/pull/80#discussion_r3203303435",
    "comments": [
      {
        "author": "slesaad",
        "body": "The 300 is a magic number coupled to the `.ui-panel` CSS `transition: 0.3s`. If the transition duration changes, this drifts. `setTimeout` is also a lower bound (throttling pushes it later), stacks on rapid state changes (no cancellation), and the immediate dispatch forces layout thrash mid-transition.\n\nThis PR's diff actually **removes** a `ResizeObserver` that was doing this job correctly. L292-L302. Restore it on `#ui-modern-center`:\n\n```js\nthis._resizeObserver?.disconnect()\nthis._resizeObserver = new ResizeObserver(() => {\n    window.dispatchEvent(new Event('resize'))\n})\nthis._resizeObserver.observe(document.getElementById('ui-modern-center'))\n```\n\n`ResizeObserver` fires when the box actually changes, batched per frame, decoupled from CSS duration. `syncDOMState` no longer needs to dispatch resize at all.\n",
        "createdAt": "2026-05-07T17:05:31Z"
      }
    ],
    "code_context": "full",
    "code_window": "   434      syncDOMState: function (e) {\n   435          if (e && e.detail && e.detail.panels) {\n   436              panels = e.detail.panels\n   437          }\n   438  \n   439          panels.forEach(panel => {\n   440              const $panel = $(document.getElementById(panel.containerId))\n   441              if ($panel.length === 0) return\n   442  \n   443              $panel.attr('data-panel-state', panel.state)\n   444  \n   445              if (panel.activeToolId && panel.state === 'focused') {\n   446                  // Update active state on icons\n   447                  $panel.find('.ui-panel-icon-btn').removeClass('active')\n   448                  $panel.find(`.ui-panel-icon-btn[data-tool=\"${panel.activeToolId}\"]`).addClass('active')\n   449  \n   450                  // Update active state on tool cards (especially for focused state and tabbed)\n   451                  $panel.find('.ui-tool-card').removeClass('active')\n   452                  $panel.find(`.ui-tool-card[data-tool=\"${panel.activeToolId}\"]`).addClass('active')\n   453              } else {\n   454                  // Remove active highlighting when not focused\n   455                  $panel.find('.ui-panel-icon-btn').removeClass('active')\n   456              }\n   457  \n   458              if (panel.state === 'iconified') {\n   459                  $panel.css({ width: '', height: '', flex: 'none' })\n   460              } else if (panel.state === 'expanded' || panel.state === 'focused') {\n   461                  let targetSize = panel.currentSize;\n   462  \n   463                  if (!targetSize) {\n   464                      targetSize = panel.config.dimensions?.expandedSize;\n   465                  }\n   466  \n   467                  if (targetSize) {\n   468                      const region = panel.config.position\n   469                      if (region === 'left' || region === 'right') {\n   470                          $panel.css({ width: targetSize + 'px', flex: 'none' })\n   471                      } else if (region === 'top' || region === 'bottom') {\n   472                          $panel.css({ height: targetSize + 'px', flex: 'none' })\n   473                      }\n   474                  } else {\n   475                      $panel.css({ width: '', height: '', flex: '' })\n   476                  }\n   477              }\n   478          })\n   479  \n   480          if (layoutStyle === 'compact') {\n   481              window.dispatchEvent(new Event('resize'))\n   482              setTimeout(() => {\n   483                  window.dispatchEvent(new Event('resize'))\n   484              }, 300)\n   485          }\n   486      },\n   487  \n   488      /**\n   489       * Attaches mouse event listeners for panel resizing.\n   490       */\n   491      attachResizeEvents: function () {\n   492          // Clean up ALL previous event handlers\n   493          $(document).off('.uiModernResize')\n   494          $('.ui-panel-drag-handle').off('mousedown.uiModernResize')\n   495  \n   496          let isResizing = false\n   497          let startX, startY\n   498          let startWidth, startHeight\n   499          let $currentPanel = null\n   500          let currentConfig = null\n   501          let currentRegion = null\n   502  \n   503          $('.ui-panel-drag-handle').on('mousedown.uiModernResize', function (e) {\n   504              e.preventDefault()\n   505              isResizing = true\n   506  \n   507              const $handle = $(e.target)\n   508              $currentPanel = $handle.closest('.ui-panel')\n   509  \n   510              if ($handle.hasClass('ui-panel-drag-handle-left')) currentRegion = 'left'\n   511              else if ($handle.hasClass('ui-panel-drag-handle-right')) currentRegion = 'right'\n   512              else if ($handle.hasClass('ui-panel-drag-handle-top')) currentRegion = 'top'\n   513              else if ($handle.hasClass('ui-panel-drag-handle-bottom')) currentRegion = 'bottom'\n   514  \n   515              const panelId = $currentPanel.attr('id')\n   516              const panelState = panels.find(p => p.containerId === panelId)\n   517              currentConfig = panelState ? panelState.config : null\n   518  \n   519              startX = e.clientX\n   520              startY = e.clientY\n   521              startWidth = $currentPanel.outerWidth()\n   522              startHeight = $currentPanel.outerHeight()\n   523  \n   524              $('body').css('user-select', 'none').addClass('ui-is-dragging')\n   525          })\n   526  \n   527          // Define resize strategies for each region to eliminate duplication\n   528          const resizeStrategies = {\n   529              left: {\n   530                  dimension: 'width',\n   531                  getDelta: (e) => e.clientX - startX,\n   532                  getStartSize: () => startWidth\n   533              },\n   534              right: {",
    "post_comment_delta": "diff --git a/src/essence/Basics/UserInterface_/UserInterfaceModern_.js b/src/essence/Basics/UserInterface_/UserInterfaceModern_.js\nindex cff522aa..78ae81ff 100644\n--- a/src/essence/Basics/UserInterface_/UserInterfaceModern_.js\n+++ b/src/essence/Basics/UserInterface_/UserInterfaceModern_.js\n@@ -1,7 +1,8 @@\n import $ from 'jquery'\n import PanelManager_ from '../PanelManager_/PanelManager_'\n+import { mmgisAPI } from '../../mmgisAPI/mmgisAPI'\n import ToolControllerModern_ from '../ToolController_/ToolControllerModern_'\n-import { validateToolMetadata } from '../ToolController_/ToolMetadataUtils'\n+import { getValidIconClass } from '../ToolController_/ToolMetadataUtils'\n import { createLogger } from '../Logger_/Logger_'\n import './UserInterfaceModern_.css'\n \n@@ -14,36 +15,36 @@ const DEFAULT_MAX_PANEL_SIZE = 9999;\n let panels = []\n let layoutStyle = ''\n let toolLoadQueue = []\n-let boundSyncDOMState = null\n-let layoutEventListenerAdded = false\n+let cleanupLayoutListener = null\n+let _resizeObserver = null\n \n // --- DOM Builder Helpers ---\n const _createPanelIconTray = (panel) => {\n     const iconsContainer = $('<div class=\"ui-panel-icons\"></div>')\n     const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n \n-    if (toolsMetadata.length > 0) {\n-        toolsMetadata.forEach(toolMetadata => {\n-            try {\n-                validateToolMetadata(toolMetadata)\n-                const toolName = toolMetadata.name || toolMetadata.id\n-                const toolId = toolMetadata.id\n-                const iconClass = toolMetadata.icon\n-                const iconBtn = $(`<button class=\"ui-panel-icon-btn\" title=\"${toolName}\" data-tool=\"${toolId}\"><span class=\"${iconClass}\"></span></button>`)\n-                \n-                iconBtn.on('click', () => {\n-                    if (panel.state === 'focused' && panel.activeToolId === toolId) {\n-                        PanelManager_.setPanelState(panel.id, 'iconified')\n-                    } else {\n-                        PanelManager_.focusTool(panel.id, toolId)\n-                    }\n-                })\n-                iconsContainer.append(iconBtn)\n-            } catch (error) {\n-                logger.error(`Failed to create icon button for tool in panel \"${panel.id}\":`, error)\n+    toolsMetadata.forEach(toolMetadata => {\n+        const toolName = toolMetadata.name || toolMetadata.id\n+        const toolId = toolMetadata.id\n+        const iconClass = getValidIconClass(toolMetadata.icon, toolId)\n+\n+        // Use safe jQuery methods to create elements and set attributes\n+        const iconSpan = $('<span></span>').addClass(iconClass)\n+        const iconBtn = $('<button></button>')\n+            .addClass('ui-panel-icon-btn')\n+            .attr('title', toolName) // jQuery escapes attribute values\n+            .attr('data-tool', toolId)\n+            .append(iconSpan)\n+\n+        iconBtn.on('click', () => {\n+            if (panel.state === 'focused' && panel.activeToolId === toolId) {\n+                PanelManager_.setPanelState(panel.id, 'iconified')\n+            } else {\n+                PanelManager_.focusTool(panel.id, toolId)\n             }\n         })\n-    }\n+        iconsContainer.append(iconBtn)\n+    })\n     return iconsContainer\n }\n \n@@ -140,10 +141,20 @@ const UserInterfaceModern_ = {\n         layoutStyle = style\n         this.render()\n \n-        if (!layoutEventListenerAdded) {\n-            boundSyncDOMState = this.syncDOMState.bind(this)\n-            window.addEventListener('mmgis-panel-layout-changed', boundSyncDOMState)\n-            layoutEventListenerAdded = true\n+        if (!cleanupLayoutListener) {\n+            cleanupLayoutListener = mmgisAPI.on('mmgis-panel-layout-changed', this.syncDOMState.bind(this))\n+        }\n+\n+        // Set up ResizeObserver to dispatch resize events when the center map area changes size\n+        if (_resizeObserver) {\n+            _resizeObserver.disconnect()\n+        }\n+        _resizeObserver = new ResizeObserver(() => {\n+            window.dispatchEvent(new Event('resize'))\n+        })\n+        const centerElement = document.getElementById('ui-modern-center')\n+        if (centerElement) {\n+            _resizeObserver.observe(centerElement)\n         }\n     },\n \n@@ -153,10 +164,13 @@ const UserInterfaceModern_ = {\n     destroy: function () {\n         ToolControllerModern_.destroyAllTools()\n         toolLoadQueue = [] // Clear any pending loads\n-        if (layoutEventListenerAdded && boundSyncDOMState) {\n-            window.removeEventListener('mmgis-panel-layout-changed', boundSyncDOMState)\n-            boundSyncDOMState = null\n-            layoutEventListenerAdded = false\n+        if (cleanupLayoutListener) {\n+            cleanupLayoutListener()\n+            cleanupLayoutListener = null\n+        }\n+        if (_resizeObserver) {\n+            _resizeObserver.disconnect()\n+            _resizeObserver = null\n         }\n         $('#modern-content').empty()\n         panels = []\n@@ -174,26 +188,29 @@ const UserInterfaceModern_ = {\n      * @throws {Error} If toolMetadata is invalid or panelId is missing\n      */\n     createToolCard: function (toolMetadata, panelId, layoutType = 'stacked', isActive = false) {\n-        validateToolMetadata(toolMetadata)\n-\n         if (!panelId) {\n             throw new Error('[UserInterfaceModern] panelId is required for createToolCard')\n         }\n \n         const toolId = toolMetadata.id\n         const targetId = `${panelId}-tool-${toolId}`\n-        const activeClass = isActive ? 'active' : ''\n         const layoutClass = layoutType === 'tabbed' ? 'ui-tool-tab-content' : 'ui-tool-card-stacked'\n \n-        const toolCard = $(`<div class=\"ui-tool-card ${layoutClass} ${activeClass}\" data-tool=\"${toolId}\" id=\"${targetId}\"></div>`)\n+        const toolCard = $('<div></div>')\n+            .addClass('ui-tool-card')\n+            .addClass(layoutClass)\n+            .addClass(isActive ? 'active' : '')\n+            .attr('data-tool', toolId)\n+            .attr('id', targetId)\n \n         // Return card and a function to load the tool (called after append)\n         // The load function checks if DOM element still exists before loading\n         return {\n             toolCard: toolCard,\n             loadTool: () => {\n-                const $target = $(`#${targetId}`)\n-                if ($target.length === 0) {\n+\n+                const targetElement = document.getElementById(targetId)\n+                if (!targetElement) {\n                     logger.warn(`Target element \"${targetId}\" not found in DOM, skipping tool load`)\n                     return\n                 }\n@@ -218,37 +235,31 @@ const UserInterfaceModern_ = {\n         const tabBar = $('<div class=\"ui-panel-tabs\"></div>')\n         const tabContentArea = $('<div class=\"ui-panel-tab-content-area\"></div>')\n \n-        // Get tool metadata from PanelManager\n-        let toolsMetadata = []\n-        try {\n-            toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n-        } catch (error) {\n-            logger.error(`Failed to get tools for panel \"${panel.id}\":`, error)\n-            return\n-        }\n+        const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n \n         toolsMetadata.forEach((toolMetadata, idx) => {\n-            try {\n-                validateToolMetadata(toolMetadata)\n-\n-                const toolName = toolMetadata.name || toolMetadata.id\n-                const toolId = toolMetadata.id\n-                const isActive = idx === 0\n-\n-                // Create tab\n-                const iconClass = toolMetadata.icon\n-                const tab = $(`<div class=\"ui-panel-tab ${isActive ? 'active' : ''}\" data-tool=\"${toolId}\"></div>`)\n-                    .append(`<span class=\"ui-tool-icon ${iconClass}\"></span>`)\n-                    .append($('<span></span>').text(toolName))\n-                tabBar.append(tab)\n-\n-                // Create tab content container and queue tool loading\n-                const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId, 'tabbed', isActive)\n-                tabContentArea.append(toolCard)\n-                toolLoadQueue.push(loadTool)\n-            } catch (error) {\n-                logger.error(`Failed to render tool in tabbed layout (panel: ${panel.id}):`, error)\n-            }\n+            const toolName = toolMetadata.name || toolMetadata.id\n+            const toolId = toolMetadata.id\n+            const isActive = idx === 0\n+\n+            // Create tab using safe jQuery methods\n+            const iconClass = getValidIconClass(toolMetadata.icon, toolId)\n+            const iconSpan = $('<span></span>').addClass('ui-tool-icon').addClass(iconClass)\n+            const textSpan = $('<span></span>').text(toolName) // .text() is safe - auto-escapes\n+\n+            const tab = $('<div></div>')\n+                .addClass('ui-panel-tab')\n+                .addClass(isActive ? 'active' : '')\n+                .attr('data-tool', toolId)\n+                .append(iconSpan)\n+                .append(textSpan)\n+\n+            tabBar.append(tab)\n+\n+            // Create tab content container and queue tool loading\n+            const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId, 'tabbed', isActive)\n+            tabContentArea.append(toolCard)\n+            toolLoadQueue.push(loadTool)\n         })\n \n         body.append(tabBar).append(tabContentArea)\n@@ -264,7 +275,9 @@ const UserInterfaceModern_ = {\n \n             // Update active content\n             tabContentArea.find('.ui-tool-tab-content').removeClass('active')\n-            tabContentArea.find(`.ui-tool-tab-content[data-tool=\"${targetTool}\"]`).addClass('active')\n+            tabContentArea.find('.ui-tool-tab-content').filter((_, element) => {\n+                return $(element).data('tool') === targetTool\n+            }).addClass('active')\n         })\n     },\n \n@@ -279,25 +292,13 @@ const UserInterfaceModern_ = {\n             return\n         }\n \n-        // Get tool metadata from PanelManager\n-        let toolsMetadata = []\n-        try {\n-            toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n-        } catch (error) {\n-            logger.error(`Failed to get tools for panel \"${panel.id}\":`, error)\n-            return\n-        }\n+        const toolsMetadata = PanelManager_.getToolsForPanel(panel.id) || []\n \n         toolsMetadata.forEach(toolMetadata => {\n-            try {\n-                const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId)\n-                body.append(toolCard)\n-                // Queue tool loading for after DOM is fully rendered\n-                toolLoadQueue.push(loadTool)\n-            } catch (error) {\n-                logger.error(`Failed to render tool in stacked layout (panel: ${panel.id}):`, error)\n-                // Skip this tool and continue with others\n-            }\n+            const { toolCard, loadTool } = this.createToolCard(toolMetadata, panel.containerId)\n+            body.append(toolCard)\n+            // Queue tool loading for after DOM is fully rendered\n+            toolLoadQueue.push(loadTool)\n         })\n     },\n \n@@ -429,11 +430,11 @@ const UserInterfaceModern_ = {\n \n     /**\n      * Passively synchronize DOM based on event payload without destroying nodes.\n-     * @param {CustomEvent} e Custom layout changed event fired from PanelManager\n+     * @param {Object} e Layout changed event payload\n      */\n     syncDOMState: function (e) {\n-        if (e && e.detail && e.detail.panels) {\n-            panels = e.detail.panels\n+        if (e && e.panels) {\n+            this.panels = e.panels\n         }\n \n         panels.forEach(panel => {\n@@ -444,12 +445,17 @@ const UserInterfaceModern_ = {\n \n             if (panel.activeToolId && panel.state === 'focused') {\n                 // Update active state on icons\n+                const activeToolId = panel.activeToolId\n                 $panel.find('.ui-panel-icon-btn').removeClass('active')\n-                $panel.find(`.ui-panel-icon-btn[data-tool=\"${panel.activeToolId}\"]`).addClass('active')\n+                $panel.find('.ui-panel-icon-btn').filter((_, element) => {\n+                    return $(element).data('tool') === activeToolId\n+                }).addClass('active')\n \n                 // Update active state on tool cards (especially for focused state and tabbed)\n                 $panel.find('.ui-tool-card').removeClass('active')\n-                $panel.find(`.ui-tool-card[data-tool=\"${panel.activeToolId}\"]`).addClass('active')\n+                $panel.find('.ui-tool-card').filter((_, element) => {\n+                    return $(element).data('tool') === activeToolId\n+                }).addClass('active')\n             } else {\n                 // Remove active highlighting when not focused\n                 $panel.find('.ui-panel-icon-btn').removeClass('active')\n@@ -476,13 +482,6 @@ const UserInterfaceModern_ = {\n                 }\n             }\n         })\n-\n-        if (layoutStyle === 'compact') {\n-            window.dispatchEvent(new Event('resize'))\n-            setTimeout(() => {\n-                window.dispatchEvent(new Event('resize'))\n-            }, 300)\n-        }\n     },\n \n     /**",
    "pr_commits_after": [
      "3842850f  Improve metadata validation and add sanitization to prevent XSS attacks",
      "71a365a5  Add dompurify package for sanitization",
      "9aea9038  Merge branch 'epic/modern-ui' into feat/render-tools",
      "268b2d41  Refactor title tool as a react component",
      "549dc9a0  Rename loadAndAssignTools to assignToolsToPanels for clarity",
      "0b3570e1  Update event handler for layout change",
      "7b96fec0  Refactor cleanup process in ModernInterface to include UserInterfaceModern destruction",
      "8125e757  Add ResizeObserver to handle dynamic resizing of the center map area",
      "883044fc  Improve duplicate tool handling in buildToolConfigMap with clearer logging"
    ]
  },
  {
    "pr": 80,
    "thread_id": "inline-3203790266",
    "source": "inline",
    "resolved": false,
    "outdated": true,
    "path": "src/essence/Basics/ToolController_/ToolControllerModern_.js",
    "line": 71,
    "commit": "e266fe7c85e01f8888065475d72db7f1253b241f",
    "diff_hunk": "@@ -1,58 +1,74 @@\n+import PanelManager_ from '../PanelManager_/PanelManager_'\n+import { toolModules } from '../../../pre/tools'\n+import { generateToolMetadata } from './ToolMetadataUtils'\n+import { createLogger } from '../Logger_/Logger_'\n+\n+const logger = createLogger('ToolControllerModern')\n+\n+// --- Module-Level State ---\n /**\n- * @typedef {import('../../types/dashboard').ToolConfig} ToolConfig\n+ * Map of loaded tool instances: targetId -> { toolInstance, toolName, toolId }\n  */\n+const loadedTools = new Map()\n \n-import PanelManager_ from '../PanelManager_/PanelManager_'\n-import { TOOL_ORIENTATION } from './types/tool'\n-import { PANEL_POSITION } from '../PanelManager_/types/layout'\n+// --- Internal Helper Functions ---\n \n-const ToolControllerModern_ = {\n-    /**\n-     * Generate tool metadata from mission config tool definition\n-     * This is a mock implementation until proper tool metadata is available\n-     *\n-     * @param {ToolConfig} toolConfig - Tool configuration from mission config\n-     * @returns {Object} Tool metadata object\n-     */\n-    generateToolMetadata: function (toolConfig) {\n-        // Default tool metadata based on common patterns\n-        const toolName = toolConfig.name || 'Unknown'\n-        const toolId = toolConfig.js || toolName.toLowerCase().replace(/\\s+/g, '-')\n-\n-        // Mock metadata - In the future, this should come from the tool's actual metadata\n-        return {\n-            id: toolId,\n-            name: toolName,\n-            // Most tools can adapt to any orientation for now\n-            // This should be refined based on actual tool requirements\n-            requiredOrientation: TOOL_ORIENTATION.ANY,\n-            icon: toolConfig.icon || 'cog',\n-            // Tools can be placed in any position by default\n-            compatiblePositions: undefined\n+/**\n+ * Finds a compatible panel for a tool, trying preferred position first\n+ * @param {Object} metadata Tool metadata\n+ * @param {Array} registeredPanels List of available panels\n+ * @returns {string|null} Panel ID if found, otherwise null\n+ */\n+const _findPanel = (metadata, registeredPanels) => {\n+    // Try preferred position first if specified\n+    if (metadata.preferredPosition) {\n+        const preferredPanel = registeredPanels.find(p =>\n+            p.config.position === metadata.preferredPosition &&\n+            PanelManager_.isToolCompatible(p.id, metadata)\n+        )\n+\n+        if (preferredPanel) {\n+            logger.debug(`Assigning \"${metadata.name}\" to preferred panel \"${preferredPanel.id}\"`)\n+            return preferredPanel.id\n         }\n-    },\n+    }\n+\n+    // Fall back to any compatible panel\n+    const compatiblePanel = registeredPanels.find(p =>\n+        PanelManager_.isToolCompatible(p.id, metadata)\n+    )\n+\n+    if (compatiblePanel) {\n+        logger.debug(`Assigning \"${metadata.name}\" to compatible panel \"${compatiblePanel.id}\"`)\n+        return compatiblePanel.id\n+    }\n+\n+    return null\n+}\n+\n+const ToolControllerModern_ = {\n \n     /**\n      * Build tool configuration maps for efficient lookup\n-     * @param {ToolConfig[]} tools - Array of tool configurations\n+     * @param {Array} tools - Array of tool configurations\n      * @returns {Object} Object containing toolConfigMap and getToolData helper\n      */\n     buildToolConfigMap: function (tools) {\n         const toolConfigMap = new Map() // id -> { config, metadata }\n         const toolNameToId = new Map() // name -> id\n \n         tools.forEach(toolConfig => {\n-            const toolMetadata = ToolControllerModern_.generateToolMetadata(toolConfig)\n+            const toolMetadata = generateToolMetadata(toolConfig)\n \n             // Store by ID (primary key)\n             if (toolConfigMap.has(toolMetadata.id)) {\n-                console.warn(`[Modern Layout] Duplicate tool ID detected: ${toolMetadata.id}`)\n+                logger.warn(`Duplicate tool ID detected: ${toolMetadata.id}`)\n             }\n             toolConfigMap.set(toolMetadata.id, { config: toolConfig, metadata: toolMetadata })\n \n             // Map name to ID for lookup\n             if (toolNameToId.has(toolMetadata.name)) {\n-                console.warn(`[Modern Layout] Duplicate tool name detected: ${toolMetadata.name}`)\n+                logger.warn(`Duplicate tool name detected: ${toolMetadata.name}`)",
    "url": "https://github.com/NASA-IMPACT/MMGIS/pull/80#discussion_r3203790266",
    "comments": [
      {
        "author": "slesaad",
        "body": "these just warn and do nothing, probably skip if duplicate?",
        "createdAt": "2026-05-07T18:21:40Z"
      }
    ],
    "code_context": "full",
    "code_window": "    21   */\n    22  const _findPanel = (metadata, registeredPanels) => {\n    23      // Try preferred position first if specified\n    24      if (metadata.preferredPosition) {\n    25          const preferredPanel = registeredPanels.find(p =>\n    26              p.config.position === metadata.preferredPosition &&\n    27              PanelManager_.isToolCompatible(p.id, metadata)\n    28          )\n    29  \n    30          if (preferredPanel) {\n    31              logger.debug(`Assigning \"${metadata.name}\" to preferred panel \"${preferredPanel.id}\"`)\n    32              return preferredPanel.id\n    33          }\n    34      }\n    35  \n    36      // Fall back to any compatible panel\n    37      const compatiblePanel = registeredPanels.find(p =>\n    38          PanelManager_.isToolCompatible(p.id, metadata)\n    39      )\n    40  \n    41      if (compatiblePanel) {\n    42          logger.debug(`Assigning \"${metadata.name}\" to compatible panel \"${compatiblePanel.id}\"`)\n    43          return compatiblePanel.id\n    44      }\n    45  \n    46      return null\n    47  }\n    48  \n    49  const ToolControllerModern_ = {\n    50  \n    51      /**\n    52       * Build tool configuration maps for efficient lookup\n    53       * @param {Array} tools - Array of tool configurations\n    54       * @returns {Object} Object containing toolConfigMap and getToolData helper\n    55       */\n    56      buildToolConfigMap: function (tools) {\n    57          const toolConfigMap = new Map() // id -> { config, metadata }\n    58          const toolNameToId = new Map() // name -> id\n    59  \n    60          tools.forEach(toolConfig => {\n    61              const toolMetadata = generateToolMetadata(toolConfig)\n    62  \n    63              // Store by ID (primary key)\n    64              if (toolConfigMap.has(toolMetadata.id)) {\n    65                  logger.warn(`Duplicate tool ID detected: ${toolMetadata.id}`)\n    66              }\n    67              toolConfigMap.set(toolMetadata.id, { config: toolConfig, metadata: toolMetadata })\n    68  \n    69              // Map name to ID for lookup\n    70              if (toolNameToId.has(toolMetadata.name)) {\n    71                  logger.warn(`Duplicate tool name detected: ${toolMetadata.name}`)\n    72              }\n    73              toolNameToId.set(toolMetadata.name, toolMetadata.id)\n    74          })\n    75  \n    76          // Helper function to get tool data by name or ID\n    77          const getToolData = (nameOrId) => {\n    78              const id = toolNameToId.get(nameOrId) || nameOrId\n    79              return toolConfigMap.get(id)\n    80          }\n    81  \n    82          return { toolConfigMap, getToolData }\n    83      },\n    84  \n    85      /**\n    86       * Assign tools explicitly defined in panel configurations\n    87       *\n    88       * @param {Array} registeredPanels - List of registered panels\n    89       * @param {Function} getToolData - Function to get tool data by name or ID\n    90       * @param {Set} assignedToolIds - Set to track assigned tool IDs\n    91       */\n    92      assignExplicitTools: function (registeredPanels, getToolData, assignedToolIds) {\n    93          registeredPanels.forEach(panelState => {\n    94              const panelConfig = panelState.config\n    95              const panelTools = panelConfig.panelTools || []\n    96  \n    97              if (panelTools.length > 0) {\n    98                  logger.debug(`Explicit assignment for panel \"${panelConfig.id}\":`, panelTools)\n    99              }\n   100  \n   101              panelTools.forEach(toolIdentifier => {\n   102                  const toolData = getToolData(toolIdentifier)\n   103  \n   104                  if (!toolData) {\n   105                      logger.warn(`Tool \"${toolIdentifier}\" specified in panel \"${panelConfig.id}\" not found in tools config`)\n   106                      return\n   107                  }\n   108  \n   109                  try {\n   110                      const { metadata } = toolData\n   111  \n   112                      // Check compatibility\n   113                      if (!PanelManager_.isToolCompatible(panelConfig.id, metadata)) {\n   114                          logger.warn(`Tool \"${metadata.name}\" is not compatible with panel \"${panelConfig.id}\"`)\n   115                          return\n   116                      }\n   117  \n   118                      // Add tool to panel\n   119                      PanelManager_.addToolToPanel(panelConfig.id, metadata)\n   120                      assignedToolIds.add(metadata.id)\n   121                  } catch (error) {",
    "post_comment_delta": "diff --git a/src/essence/Basics/ToolController_/ToolControllerModern_.js b/src/essence/Basics/ToolController_/ToolControllerModern_.js\nindex 9028fac0..c59d407d 100644\n--- a/src/essence/Basics/ToolController_/ToolControllerModern_.js\n+++ b/src/essence/Basics/ToolController_/ToolControllerModern_.js\n@@ -62,15 +62,17 @@ const ToolControllerModern_ = {\n \n             // Store by ID (primary key)\n             if (toolConfigMap.has(toolMetadata.id)) {\n-                logger.warn(`Duplicate tool ID detected: ${toolMetadata.id}`)\n+                logger.warn(`Duplicate tool ID detected: ${toolMetadata.id} - skipping duplicate tool`)\n+                return\n             }\n             toolConfigMap.set(toolMetadata.id, { config: toolConfig, metadata: toolMetadata })\n \n             // Map name to ID for lookup\n             if (toolNameToId.has(toolMetadata.name)) {\n-                logger.warn(`Duplicate tool name detected: ${toolMetadata.name}`)\n+                logger.warn(`Duplicate tool name detected: ${toolMetadata.name} - tool can only be referenced by ID`)\n+            } else {\n+                toolNameToId.set(toolMetadata.name, toolMetadata.id)\n             }\n-            toolNameToId.set(toolMetadata.name, toolMetadata.id)\n         })\n \n         // Helper function to get tool data by name or ID\n@@ -178,14 +180,14 @@ const ToolControllerModern_ = {\n     },\n \n     /**\n-     * Load and assign tools to panels based on dashboard configuration\n+     * Assign tools to panels based on dashboard configuration\n      * Tools are assigned in two passes:\n      * 1. Explicit assignment via panel.tools arrays\n      * 2. Fallback assignment for unassigned tools (if tool.on !== false)\n      *\n      * @param {Array} tools - Array of tool configurations from mission config\n      */\n-    loadAndAssignTools: function (tools) {\n+    assignToolsToPanels: function (tools) {\n         if (!tools || tools.length === 0) {\n             logger.warn('No tools configured')\n             return",
    "pr_commits_after": [
      "3842850f  Improve metadata validation and add sanitization to prevent XSS attacks",
      "71a365a5  Add dompurify package for sanitization",
      "9aea9038  Merge branch 'epic/modern-ui' into feat/render-tools",
      "268b2d41  Refactor title tool as a react component",
      "549dc9a0  Rename loadAndAssignTools to assignToolsToPanels for clarity",
      "0b3570e1  Update event handler for layout change",
      "7b96fec0  Refactor cleanup process in ModernInterface to include UserInterfaceModern destruction",
      "8125e757  Add ResizeObserver to handle dynamic resizing of the center map area",
      "883044fc  Improve duplicate tool handling in buildToolConfigMap with clearer logging"
    ]
  },
  {
    "pr": 80,
    "thread_id": "inline-3203799256",
    "source": "inline",
    "resolved": false,
    "outdated": false,
    "path": "src/essence/Basics/ToolController_/ToolControllerModern_.js",
    "line": 341,
    "commit": "e266fe7c85e01f8888065475d72db7f1253b241f",
    "diff_hunk": "@@ -172,11 +196,189 @@ const ToolControllerModern_ = {\n         const registeredPanels = PanelManager_.getAllPanelsByPriority()\n         const assignedToolIds = new Set()\n \n-        // First pass: Assign tools based on panel.tools array\n+        logger.debug('Starting tool assignment...')\n+        logger.debug(`${tools.length} tools configured, ${registeredPanels.length} panels registered`)\n+\n+        // First pass: Assign tools based on panel.toolNames array\n         ToolControllerModern_.assignExplicitTools(registeredPanels, getToolData, assignedToolIds)\n \n         // Second pass: Assign remaining tools to compatible panels (fallback)\n         ToolControllerModern_.assignFallbackTools(registeredPanels, toolConfigMap, assignedToolIds)\n+    },\n+\n+    // ========================================================================\n+    // Tool Lifecycle Management (Loading, Instantiation, Cleanup)\n+    // ========================================================================\n+\n+    /**\n+     * Load and instantiate a tool in a specific target container\n+     *\n+     * @param {Object} toolMetadata - Tool metadata object\n+     * @param {string} toolMetadata.id - Tool identifier (e.g., 'TitleTool')\n+     * @param {string} toolMetadata.name - Tool display name (e.g., 'Title')\n+     * @param {string} targetId - DOM element ID where tool should render\n+     * @returns {Object|null} Tool instance or null if failed\n+     */\n+    loadTool: function (toolMetadata, targetId) {\n+        // Validate inputs\n+        if (!toolMetadata || typeof toolMetadata !== 'object') {\n+            logger.error('Invalid tool metadata:', toolMetadata)\n+            return null\n+        }\n+\n+        if (!toolMetadata.id || typeof toolMetadata.id !== 'string') {\n+            logger.error('Tool metadata missing required \"id\" field:', toolMetadata)\n+            return null\n+        }\n+\n+        if (!targetId || typeof targetId !== 'string') {\n+            logger.error('Missing or invalid targetId for tool:', toolMetadata.name)\n+            return null\n+        }\n+\n+        // Verify DOM element exists (defense-in-depth with UserInterfaceModern_)\n+        const targetElement = document.getElementById(targetId)\n+        if (!targetElement) {\n+            logger.error(`Target element \"${targetId}\" not found in DOM for tool \"${toolMetadata.name}\"`)\n+            return null\n+        }\n+\n+        try {\n+            // Find the tool module in pre/tools.js exports\n+            const ToolClass = toolModules[toolMetadata.id]\n+\n+            if (!ToolClass) {\n+                logger.error(\n+                    `Tool module \"${toolMetadata.id}\" not found in toolModules.`,\n+                    'Available tools:',\n+                    Object.keys(toolModules)\n+                )\n+                return null\n+            }\n+\n+            // Check if tool is already loaded in this container\n+            if (loadedTools.has(targetId)) {\n+                const existing = loadedTools.get(targetId)\n+                logger.warn(\n+                    `Tool already loaded in container \"${targetId}\":`,\n+                    existing.toolName,\n+                    '- destroying before loading new tool'\n+                )\n+                this.destroyTool(targetId)\n+            }\n+\n+            // Initialize tool if it has an initialize method\n+            if (typeof ToolClass.initialize === 'function') {\n+                ToolClass.initialize()\n+            }\n+\n+            // Call make() to render the tool\n+            if (typeof ToolClass.make === 'function') {\n+                ToolClass.make(targetId)\n+            } else {\n+                logger.warn(`Tool \"${toolMetadata.name}\" does not have a make() method`)\n+            }\n+\n+            // Track the loaded tool instance\n+            loadedTools.set(targetId, {\n+                toolInstance: ToolClass,\n+                toolName: toolMetadata.name,\n+                toolId: toolMetadata.id,\n+                targetId: targetId\n+            })\n+\n+            logger.debug(`Loaded tool \"${toolMetadata.name}\" in container \"${targetId}\"`)\n+\n+            return ToolClass\n+        } catch (error) {\n+            logger.error(`Failed to load tool \"${toolMetadata.name}\":`, error)\n+            return null\n+        }\n+    },\n+\n+    /**\n+     * Destroy a tool instance in a specific container\n+     *\n+     * @param {string} targetId - DOM element ID of the tool container\n+     * @returns {boolean} True if destroyed successfully\n+     */\n+    destroyTool: function (targetId) {\n+        if (!loadedTools.has(targetId)) {\n+            return false\n+        }\n+\n+        try {\n+            const { toolInstance, toolName } = loadedTools.get(targetId)\n+\n+            // Call destroy() if available\n+            if (typeof toolInstance.destroy === 'function') {\n+                toolInstance.destroy()\n+            }\n+\n+            // Remove from tracking\n+            loadedTools.delete(targetId)\n+\n+            logger.debug(`Destroyed tool \"${toolName}\" from container \"${targetId}\"`)\n+\n+            return true\n+        } catch (error) {\n+            logger.error(`Error destroying tool in container \"${targetId}\":`, error)\n+            return false\n+        }\n+    },\n+\n+    /**\n+     * Destroy all loaded tools\n+     */\n+    destroyAllTools: function () {\n+        const targetIds = Array.from(loadedTools.keys())\n+\n+        logger.debug(`Destroying ${targetIds.length} loaded tools`)\n+\n+        targetIds.forEach(targetId => {\n+            this.destroyTool(targetId)\n+        })\n+    },",
    "url": "https://github.com/NASA-IMPACT/MMGIS/pull/80#discussion_r3203799256",
    "comments": [
      {
        "author": "slesaad",
        "body": "should this also unregister from PanelManager?",
        "createdAt": "2026-05-07T18:23:06Z"
      },
      {
        "author": "sandesh-sp",
        "body": "I don't think it should unregistered from PanelManager as destroyTool is only concerned with runtime instance. We may need a separate unassignFromPanelManager function though when we dynamically add or remove plugins from the panel.",
        "createdAt": "2026-05-15T16:15:25Z"
      }
    ],
    "code_context": "full",
    "code_window": "   291  \n   292              return ToolClass\n   293          } catch (error) {\n   294              logger.error(`Failed to load tool \"${toolMetadata.name}\":`, error)\n   295              return null\n   296          }\n   297      },\n   298  \n   299      /**\n   300       * Destroy a tool instance in a specific container\n   301       *\n   302       * @param {string} targetId - DOM element ID of the tool container\n   303       * @returns {boolean} True if destroyed successfully\n   304       */\n   305      destroyTool: function (targetId) {\n   306          if (!loadedTools.has(targetId)) {\n   307              return false\n   308          }\n   309  \n   310          try {\n   311              const { toolInstance, toolName } = loadedTools.get(targetId)\n   312  \n   313              // Call destroy() if available\n   314              if (typeof toolInstance.destroy === 'function') {\n   315                  toolInstance.destroy()\n   316              }\n   317  \n   318              // Remove from tracking\n   319              loadedTools.delete(targetId)\n   320  \n   321              logger.debug(`Destroyed tool \"${toolName}\" from container \"${targetId}\"`)\n   322  \n   323              return true\n   324          } catch (error) {\n   325              logger.error(`Error destroying tool in container \"${targetId}\":`, error)\n   326              return false\n   327          }\n   328      },\n   329  \n   330      /**\n   331       * Destroy all loaded tools\n   332       */\n   333      destroyAllTools: function () {\n   334          const targetIds = Array.from(loadedTools.keys())\n   335  \n   336          logger.debug(`Destroying ${targetIds.length} loaded tools`)\n   337  \n   338          targetIds.forEach(targetId => {\n   339              this.destroyTool(targetId)\n   340          })\n   341      },\n   342  \n   343      /**\n   344       * Get a loaded tool instance by target ID\n   345       *\n   346       * @param {string} targetId - DOM element ID of the tool container\n   347       * @returns {Object|null} Tool data or null if not found\n   348       */\n   349      getTool: function (targetId) {\n   350          return loadedTools.get(targetId) || null\n   351      },\n   352  \n   353      /**\n   354       * Check if a tool is loaded in a specific container\n   355       *\n   356       * @param {string} targetId - DOM element ID of the tool container\n   357       * @returns {boolean} True if tool is loaded\n   358       */\n   359      isToolLoaded: function (targetId) {\n   360          return loadedTools.has(targetId)\n   361      },\n   362  \n   363      /**\n   364       * Get all loaded tools\n   365       *\n   366       * @returns {Array} Array of loaded tool data objects\n   367       */\n   368      getAllLoadedTools: function () {\n   369          return Array.from(loadedTools.values())\n   370      },\n   371  \n   372      /**\n   373       * Reload a tool in its container (destroy + load)\n   374       *\n   375       * @param {Object} toolMetadata - Tool metadata object\n   376       * @param {string} targetId - DOM element ID where tool should render\n   377       * @returns {Object|null} Tool instance or null if failed\n   378       */\n   379      reloadTool: function (toolMetadata, targetId) {\n   380          this.destroyTool(targetId)\n   381          return this.loadTool(toolMetadata, targetId)\n   382      }\n   383  }\n   384  \n   385  export default ToolControllerModern_",
    "post_comment_delta": "diff --git a/src/essence/Basics/ToolController_/ToolControllerModern_.js b/src/essence/Basics/ToolController_/ToolControllerModern_.js\nindex 9028fac0..c59d407d 100644\n--- a/src/essence/Basics/ToolController_/ToolControllerModern_.js\n+++ b/src/essence/Basics/ToolController_/ToolControllerModern_.js\n@@ -62,15 +62,17 @@ const ToolControllerModern_ = {\n \n             // Store by ID (primary key)\n             if (toolConfigMap.has(toolMetadata.id)) {\n-                logger.warn(`Duplicate tool ID detected: ${toolMetadata.id}`)\n+                logger.warn(`Duplicate tool ID detected: ${toolMetadata.id} - skipping duplicate tool`)\n+                return\n             }\n             toolConfigMap.set(toolMetadata.id, { config: toolConfig, metadata: toolMetadata })\n \n             // Map name to ID for lookup\n             if (toolNameToId.has(toolMetadata.name)) {\n-                logger.warn(`Duplicate tool name detected: ${toolMetadata.name}`)\n+                logger.warn(`Duplicate tool name detected: ${toolMetadata.name} - tool can only be referenced by ID`)\n+            } else {\n+                toolNameToId.set(toolMetadata.name, toolMetadata.id)\n             }\n-            toolNameToId.set(toolMetadata.name, toolMetadata.id)\n         })\n \n         // Helper function to get tool data by name or ID\n@@ -178,14 +180,14 @@ const ToolControllerModern_ = {\n     },\n \n     /**\n-     * Load and assign tools to panels based on dashboard configuration\n+     * Assign tools to panels based on dashboard configuration\n      * Tools are assigned in two passes:\n      * 1. Explicit assignment via panel.tools arrays\n      * 2. Fallback assignment for unassigned tools (if tool.on !== false)\n      *\n      * @param {Array} tools - Array of tool configurations from mission config\n      */\n-    loadAndAssignTools: function (tools) {\n+    assignToolsToPanels: function (tools) {\n         if (!tools || tools.length === 0) {\n             logger.warn('No tools configured')\n             return",
    "pr_commits_after": [
      "3842850f  Improve metadata validation and add sanitization to prevent XSS attacks",
      "71a365a5  Add dompurify package for sanitization",
      "9aea9038  Merge branch 'epic/modern-ui' into feat/render-tools",
      "268b2d41  Refactor title tool as a react component",
      "549dc9a0  Rename loadAndAssignTools to assignToolsToPanels for clarity",
      "0b3570e1  Update event handler for layout change",
      "7b96fec0  Refactor cleanup process in ModernInterface to include UserInterfaceModern destruction",
      "8125e757  Add ResizeObserver to handle dynamic resizing of the center map area",
      "883044fc  Improve duplicate tool handling in buildToolConfigMap with clearer logging"
    ]
  }
]
