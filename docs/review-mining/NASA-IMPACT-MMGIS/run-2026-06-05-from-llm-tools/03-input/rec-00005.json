{"pr":111,"thread_id":"inline-3350433246","source":"inline","resolved":false,"outdated":false,"path":"API/Backend/CardPlugin/setup.js","line":9,"commit":"b379f282de017223382ab9a8eed4cd9cf53b6831","diff_hunk":"@@ -0,0 +1,24 @@\n+const router = require('./routes/upload');\n+\n+let setup = {\n+    // Once the app initializes\n+    onceInit: (s) => {\n+        // Image upload is a Configure-page (admin) action, so it uses the same\n+        // authorization posture as the /api/configure routes (see\n+        // API/Backend/Config/setup.js): ensureAdmin gates it to admin/lead\n+        // sessions (and passes under AUTH=none), not ensureUser/stopGuests.","url":"https://github.com/NASA-IMPACT/MMGIS/pull/111#discussion_r3350433246","comments":[{"author":"CarsonDavis","body":"is it true that it passes under AUTH=none? i think none only allows viewing not writes","createdAt":"2026-06-03T17:02:01Z"}],"code_context":"full","code_window":"     1  const router = require('./routes/upload');\n     2  \n     3  let setup = {\n     4      // Once the app initializes\n     5      onceInit: (s) => {\n     6          // Image upload is a Configure-page (admin) action, so it uses the same\n     7          // authorization posture as the /api/configure routes (see\n     8          // API/Backend/Config/setup.js): ensureAdmin gates it to admin/lead\n     9          // sessions (and passes under AUTH=none), not ensureUser/stopGuests.\n    10          s.app.use(\n    11              s.ROOT_PATH + '/api/cardplugin',\n    12              s.ensureAdmin(),\n    13              s.checkHeadersCodeInjection,\n    14              s.setContentType,\n    15              router\n    16          );\n    17      },\n    18      // Once the server starts\n    19      onceStarted: (s) => {},\n    20      // Once all tables sync\n    21      onceSynced: (s) => {},\n    22  };\n    23  \n    24  module.exports = setup;","post_comment_delta":"","pr_commits_after":[]}
