# Copy to backend.hcl and fill in, then: terraform init -backend-config=backend.hcl
# Production's state bucket is created by the bootstrap root (terraform/bootstrap).
bucket = "mmgis-production-tfstate-<ACCOUNT_ID>"
region = "us-west-2"
